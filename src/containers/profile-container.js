import React from 'react';
import {connect} from 'react-redux';
import * as profileAction from '../actions/profile-actions';

class ProfileReducer extends React.Component {

}

let mapStateToProps = () => ({

});

export default connect(mapStateToProps, profileAction)(ProfileReducer);
